# {设计标题} — 总设计文档

> 最后更新: {YYYY-MM-DD}
> 需求来源: {用户需求原文摘要}
> 整体状态: ⏳ 待实现（由各模块状态自动派生）
> 图例: ⏳ 待实现 | 🔄 待迭代 | 🧪 待测试 | ✅ 已完成

## 1. 背景与目标

**需求背景**: {为什么要做}

**预期结果**: {做完之后系统会变成什么样}

## 2. 范围 / 非范围

**In Scope**:
- {要做的事项}

**Out of Scope**:
- {明确不做的事项}

## 3. 现状与代码证据

| 文件路径 | 关键符号 | 行号范围 | 当前行为摘要 |
|----------|----------|----------|-------------|
| `{path}` | `{symbol}` | L{start}-L{end} | {摘要} |

**当前测试框架与测试组织**:
- 测试框架: {如 Jest / Vitest / pytest}
- 测试目录/命名约定: {如 `tests/**`, `*.test.ts`}

**当前组件复用入口**:
- 项目内已有组件目录: {路径}
- 组件化工具集中存放位置: {路径}

**代码规范**（从现有代码中发现，各模块实施时遵循）:
- 命名规范: {文件/变量/函数/类的命名约定}
- 代码风格: {缩进、引号、分号等，参考项目 ESLint/Prettier 配置}
- 错误处理: {现有项目如何处理异常，如 try-catch 模式、错误中间件}
- 日志规范: {日志库、日志级别、关键路径日志格式}
- 注释规范: {JSDoc/Docstring 等标准，覆盖范围}

**现有模式总结**: {架构路径、扩展点、可复用部分}

## 4. 模块索引与依赖关系

### 4.1 模块划分

| 模块 | 职责 | 文档 | 依赖模块 |
|------|------|------|----------|
| {模块A} | {职责} | [{模块A}](./{a}/index.md) | — |
| {模块B} | {职责} | [{模块B}](./{b}/index.md) | {模块A} |

### 4.2 模块依赖图

```mermaid
graph TD
    {模块B} --> {模块A}
```

> 箭头方向：依赖方 → 被依赖方（如 B 依赖 A，则 B → A）

### 4.3 循环依赖检查

> 结果: ✅ 无循环依赖 | ❌ 检测到循环依赖（{列出涉及的模块}）

- 已对依赖图执行 DFS 环检测
- 若存在环：列出环中模块，建议合并或打破某条依赖后重新设计

## 5. 跨模块方案设计

### 5.1 跨模块共享数据结构

```typescript
// 被多个模块引用的类型/接口/常量
interface {SharedType} {
  {field}: {Type}
}
```

（无则写"无"）

### 5.2 跨模块共享约定

| 约定项 | 说明 | 适用模块 |
|--------|------|----------|
| {约定} | {说明} | {模块列表} |

（无则写"无"）

## 6. 风险与回滚

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| {风险描述} | 高/中/低 | {影响范围} | {怎么缓解} |

**回滚方式**: {如何回滚到变更前状态}

## 7. 未决问题

| # | 问题 | 影响范围 | 需要谁确认 |
|---|------|----------|-----------|
| 1 | {待确认的问题} | {影响什么决策} | {角色/人} |
